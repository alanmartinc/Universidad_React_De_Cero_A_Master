import App from '../App'

describe('Grupo de pruebas de ejemplo', () => {
    test('Una prueba unitaria de algo', () => {
        expect(10).toBe(10)
    })

    test('Segunda prueba de numeros', () => {
        expect(10).toBe(10)
    })
})